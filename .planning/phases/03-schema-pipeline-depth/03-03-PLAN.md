---
phase: 03-schema-pipeline-depth
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/yune-rime-api/src/tests/schema_processors.rs
  - crates/yune-rime-api/src/tests/schema_selection.rs
  - crates/yune-rime-api/src/tests/distribution_schema_comparison.rs
  - crates/yune-rime-api/src/tests/mod.rs
autonomous: true
requirements:
  - SCHEMA-05
must_haves:
  truths:
    - "At least two larger real-world schema chains are compared directly against librime for user-visible schema semantics."
    - "Differences are converted into focused fixtures or structured findings instead of broad undiagnosed snapshots."
    - "Compiled payload and userdb gaps discovered by comparisons are assigned to Phase 4 or Phase 5 instead of shimmed in Phase 3."
  artifacts:
    - path: "crates/yune-rime-api/src/tests/distribution_schema_comparison.rs"
      provides: "Focused librime-vs-Yune distribution schema comparison tests or harness"
      contains: "librime"
    - path: "crates/yune-rime-api/src/tests/mod.rs"
      provides: "Test module registration for distribution schema comparison coverage"
      contains: "distribution_schema_comparison"
    - path: "crates/yune-rime-api/src/tests/schema_processors.rs"
      provides: "Focused processor fixtures converted from distribution comparison differences"
    - path: "crates/yune-rime-api/src/tests/schema_selection.rs"
      provides: "Focused schema-chain fixtures converted from distribution comparison differences"
  key_links:
    - from: "crates/yune-rime-api/src/tests/distribution_schema_comparison.rs"
      to: "/Users/trenton/Projects/librime"
      via: "librime compatibility oracle invocation or documented comparison fixture source"
      pattern: "librime"
    - from: "distribution comparison findings"
      to: "focused ABI fixtures"
      via: "difference categories: component order, segment tags, generated spellings, OpenCC/filter behavior, punctuation/fallback behavior, candidate differences"
      pattern: "segment_tags|candidate|punct|OpenCC"
---

<objective>
Create distribution-scale librime comparison coverage that turns larger schema-chain differences into focused Phase 3 fixtures or structured findings.

Purpose: D-08 and D-09 require direct comparison against librime for larger real-world schema chains, while D-10 requires out-of-phase gaps to be recorded rather than shimmed.
Output: A focused distribution comparison test module/harness, registration in the test suite, and converted fixtures/findings for schema-chain behavior relevant to SCHEMA-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-schema-pipeline-depth/03-CONTEXT.md
@.planning/phases/03-schema-pipeline-depth/03-RESEARCH.md
@.planning/phases/03-schema-pipeline-depth/03-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@crates/yune-rime-api/src/tests/mod.rs
@crates/yune-rime-api/src/tests/schema_processors.rs
@crates/yune-rime-api/src/tests/schema_selection.rs

<interfaces>
Executor must use these existing contracts and ownership anchors directly:

- `/Users/trenton/Projects/librime` is the compatibility oracle for user-visible behavior.
- ABI tests must drive Yune through the RIME ABI path using deployed YAML fixtures and `RimeProcessKey`, not direct `Engine` shortcuts, for frontend-visible behavior.
- Distribution comparisons should emphasize component order, segment tags, generated spellings, OpenCC/filter behavior, punctuation/fallback behavior, and candidate differences; do not create broad snapshots that are hard to diagnose.
- If a comparison reveals compiled `.table.bin`, `.prism.bin`, `.reverse.bin`, UniTE payload, or reverse-data requirements, record a Phase 4 finding. If it reveals LevelDB/userdb learning/storage requirements, record a Phase 5 finding.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a focused distribution schema comparison module and register it</name>
  <files>crates/yune-rime-api/src/tests/distribution_schema_comparison.rs, crates/yune-rime-api/src/tests/mod.rs</files>
  <read_first>
    - .planning/phases/03-schema-pipeline-depth/03-CONTEXT.md
    - .planning/phases/03-schema-pipeline-depth/03-RESEARCH.md
    - .planning/phases/03-schema-pipeline-depth/03-PATTERNS.md
    - crates/yune-rime-api/src/tests/mod.rs
    - crates/yune-rime-api/src/tests/schema_processors.rs
    - crates/yune-rime-api/src/tests/schema_selection.rs
  </read_first>
  <behavior>
    - Test 1: A distribution-style schema chain comparison captures Yune user-visible state for component order, segment tags, generated spellings, punctuation/fallback behavior, filters, and candidates through ABI calls, per D-08, D-09, and SCHEMA-05.
    - Test 2: The comparison module records or asserts librime oracle expectations for selected schemas without broad snapshot churn, per D-08.
    - Test 3: Differences outside Phase 3 are represented as structured findings with target phase fields, per D-10.
  </behavior>
  <action>Create `crates/yune-rime-api/src/tests/distribution_schema_comparison.rs` and register it with `mod distribution_schema_comparison;` in `crates/yune-rime-api/src/tests/mod.rs`. Implement a small comparison harness that can run Yune ABI sessions for two selected larger schema fixtures and store expected librime observations inline in focused assertions or test-local structs. Use schema names selected from available local fixtures or distribution schemas that do not require compiled payloads for the compared behavior; if the available real-world schema requires compiled payloads, isolate the chain semantics into deployed YAML fixtures and record the payload dependency as a Phase 4 finding in comments and summary. Include comparison categories as concrete fields: `component_order`, `segment_tags`, `generated_spellings`, `opencc_or_filter_behavior`, `punctuation_or_fallback_behavior`, and `candidate_differences`. Do not add a broad golden snapshot file.</action>
  <verify>
    <automated>PATH="/Users/trenton/.cargo/bin:$PATH" cargo test -p yune-rime-api distribution_schema_comparison -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' crates/yune-rime-api/src/tests/mod.rs | grep -c 'mod distribution_schema_comparison;'` prints `1`.
    - `grep -v '^#' crates/yune-rime-api/src/tests/distribution_schema_comparison.rs | grep -c 'component_order\|segment_tags\|generated_spellings\|opencc_or_filter_behavior\|punctuation_or_fallback_behavior\|candidate_differences'` prints at least `6`.
    - `grep -v '^#' crates/yune-rime-api/src/tests/distribution_schema_comparison.rs | grep -c 'librime'` is non-zero.
    - The new module contains no broad serialized snapshot fixture larger than the focused assertions needed by the tests.
  </acceptance_criteria>
  <done>A registered comparison module exists and compares larger schema-chain semantics against librime-derived expectations in focused, diagnosable categories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert distribution differences into focused fixtures or structured findings</name>
  <files>crates/yune-rime-api/src/tests/distribution_schema_comparison.rs, crates/yune-rime-api/src/tests/schema_processors.rs, crates/yune-rime-api/src/tests/schema_selection.rs</files>
  <read_first>
    - .planning/phases/03-schema-pipeline-depth/03-CONTEXT.md
    - crates/yune-rime-api/src/tests/distribution_schema_comparison.rs
    - crates/yune-rime-api/src/tests/schema_processors.rs
    - crates/yune-rime-api/src/tests/schema_selection.rs
    - crates/yune-rime-api/src/schema_install.rs
    - crates/yune-rime-api/src/processors/speller.rs
    - crates/yune-core/src/spelling_algebra.rs
    - crates/yune-core/src/filter/mod.rs
  </read_first>
  <behavior>
    - Focused fixture 1: A processor/segmentor difference from distribution comparison is represented in `schema_processors.rs` or remains asserted in the comparison module with a named owner.
    - Focused fixture 2: A translator/filter/schema-chain difference from distribution comparison is represented in `schema_selection.rs` or remains asserted in the comparison module with a named owner.
    - Structured finding: Every out-of-phase gap includes observed Yune behavior, expected librime behavior when known, scope decision, and target phase, per D-10.
  </behavior>
  <action>Run the comparison module and inspect each difference category. For Phase 3-owned differences, add focused ABI fixtures in `schema_processors.rs` or `schema_selection.rs` with names derived from the category and owner, such as `distribution_punctuation_fallback_order_matches_librime` or `distribution_filter_chain_tags_match_librime`. For differences that belong to Plan 03-04 spelling/OpenCC/correction work, leave a focused failing comparison in `distribution_schema_comparison.rs` and identify the owning files for Plan 03-04. For compiled dictionary, reverse, UniTE, or userdb gaps, add structured test-local finding data and record it in the summary; do not implement shims. Avoid broad snapshots and avoid changing production code in this plan unless a small test helper is required.</action>
  <verify>
    <automated>PATH="/Users/trenton/.cargo/bin:$PATH" cargo test -p yune-rime-api distribution_schema_comparison schema_processors schema_selection -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' crates/yune-rime-api/src/tests/distribution_schema_comparison.rs | grep -c 'target_phase'` is non-zero if any out-of-phase finding exists.
    - Any structured finding includes exact field names or strings `observed_yune_behavior`, `expected_librime_behavior`, `scope_decision`, and `target_phase`.
    - No new test name contains `snapshot` unless it is explicitly a focused, category-scoped assertion; broad snapshot churn is not introduced.
    - If compiled payload or userdb findings exist, their target phases are exactly `04-compiled-dictionary-data` or `05-userdb-and-learning`.
  </acceptance_criteria>
  <done>Distribution differences are either focused Phase 3 fixtures or structured findings assigned to the correct future phase.</done>
</task>

<task type="auto">
  <name>Task 3: Run comparison and workspace gates</name>
  <files>crates/yune-rime-api/src/tests/distribution_schema_comparison.rs, crates/yune-rime-api/src/tests/mod.rs, crates/yune-rime-api/src/tests/schema_processors.rs, crates/yune-rime-api/src/tests/schema_selection.rs</files>
  <read_first>
    - crates/yune-rime-api/src/tests/distribution_schema_comparison.rs
    - crates/yune-rime-api/src/tests/mod.rs
    - .planning/codebase/TESTING.md
  </read_first>
  <action>Run focused distribution comparison tests plus workspace gates. In the required summary, include selected schema names, librime oracle source, compared categories, converted focused fixtures, and structured findings. For any failed comparison intentionally left for Plan 03-04, record the exact owning module and test name so the next executor can continue without rediscovery.</action>
  <verify>
    <automated>PATH="/Users/trenton/.cargo/bin:$PATH" cargo test -p yune-rime-api distribution_schema_comparison -- --nocapture</automated>
    <automated>PATH="/Users/trenton/.cargo/bin:$PATH" cargo fmt --check</automated>
    <automated>PATH="/Users/trenton/.cargo/bin:$PATH" cargo test --workspace</automated>
    <automated>PATH="/Users/trenton/.cargo/bin:$PATH" cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - All automated commands in `<verify>` exit 0, or the summary explicitly lists intentionally failing comparison fixtures handed to Plan 03-04 with exact test names and owners.
    - `.planning/phases/03-schema-pipeline-depth/03-03-SUMMARY.md` contains `component_order`, `segment_tags`, `generated_spellings`, `OpenCC`, `punctuation`, and `candidate`.
    - The summary contains `04-compiled-dictionary-data` or `05-userdb-and-learning` for every out-of-phase payload/userdb finding.
  </acceptance_criteria>
  <done>Distribution comparison coverage is diagnosable, tied to librime, and converted into fixtures/findings without broad snapshot churn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| librime oracle artifacts -> Yune tests | Oracle observations from `/Users/trenton/Projects/librime` are external local inputs and must be recorded as data, not executed inside Yune. |
| distribution schema files -> deployed test runtime | Larger schemas can contain component names and resource identifiers; tests must preserve logical resource validation. |
| comparison results -> future phase findings | Findings influence roadmap scope; they must not overstate compatibility or hide high-severity behavior gaps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | distribution fixture loading | mitigate | Copy or synthesize focused deployed YAML fixtures into temp dirs; do not modify upstream distribution or librime directories. |
| T-03-03-02 | Repudiation | comparison findings | mitigate | Each finding must include observed Yune behavior, expected librime behavior when known, scope decision, and target phase per D-10. |
| T-03-03-03 | Denial of Service | comparison suite | mitigate | Keep tests focused on selected key sequences and categories; do not add broad exhaustive schema snapshots or long fuzz loops. |
| T-03-03-04 | Information Disclosure | local paths in test output | accept | Tests may mention local oracle path `/Users/trenton/Projects/librime`; no secrets or external services are involved. |
</threat_model>

<verification>
Overall checks:
- `PATH="/Users/trenton/.cargo/bin:$PATH" cargo test -p yune-rime-api distribution_schema_comparison -- --nocapture`
- `PATH="/Users/trenton/.cargo/bin:$PATH" cargo fmt --check`
- `PATH="/Users/trenton/.cargo/bin:$PATH" cargo test --workspace`
- `PATH="/Users/trenton/.cargo/bin:$PATH" cargo clippy --workspace --all-targets -- -D warnings`
</verification>

<success_criteria>
- SCHEMA-05 distribution-chain comparison is represented by focused tests or harness code.
- Larger schema-chain differences are compared directly against librime-derived observations.
- Phase 3-owned differences become focused fixtures; out-of-phase compiled payload/userdb gaps become structured findings targeting Phase 4 or Phase 5.
- Broad snapshot churn is avoided.
</success_criteria>

<output>
After completion, create `.planning/phases/03-schema-pipeline-depth/03-03-SUMMARY.md`.
</output>

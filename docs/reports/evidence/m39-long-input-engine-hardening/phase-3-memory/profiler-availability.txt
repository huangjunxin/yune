umdh.exe=missing
gflags.exe=missing
wpr.exe=C:\Windows\system32\wpr.exe
xperf.exe=missing

UMDH/GFlags and XPerf were not available in this workspace, so M39 could not
capture allocation stack traces with those tools. WPR is present, but the M39
benchmark does not include a repeatable WPR allocation-stack collection and
symbolization flow. The checked-in owner attribution therefore uses the deepest
available repeatable evidence: benchmark working-set/peak rows, product-path
storage status, selected heap-mirror byte counters, and M39 owner counters.
